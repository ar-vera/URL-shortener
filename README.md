# URL-shortener

Краткое описание  
Простой и лёгкий сервис для сокращения URL-адресов: принимает длинный URL, выдаёт короткий, позволяет перенаправлять по короткому на исходный адрес

---

## 📌 Возможности

- Генерация коротких ссылок из заданного длинного URL  
- Перенаправление (redirect) с короткой ссылки на исходную  
- Возможная настройка собственного префикса или шаблона  
- Сохранение истории / логов (в зависимости от реализации)  
- Юнит-тесты покрывают ключевые функции  

---

## 🧰 Технологии

- Язык: Go (Golang) — 100% кода на Go
- Структура проекта:  
  - `cmd/url-shortener` — точка входа  
  - `config/` — настройки, конфигурации  
  - `internal/` — внутренняя логика  
  - `tests/` — тесты  
- Лицензия: GPL-3.0

---

## 🚀 Быстрый старт

### Требования  
- Go 1.20+
- База данных / хранилище
- Настройка окружения (файл конфигурации или переменные окружения)  

### Установка  
```bash
git clone https://github.com/ar-vera/URL-shortener.git
cd URL-shortener
go mod download
